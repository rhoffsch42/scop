v 2018 01 05
-----------------------
logs:
	creer un fichier txt
	ecrire dedans
		faire ft_printf ?

---------------------------------
! get_next_line utilise des strdup ~like 
	-> s'arrete sur les \0
faire une fonction qui copie le contenu complet et l'envoie dans cette structure:
typedef struct		s_data
{
	struct s_data	*next;
	int				len;
	char			*data;
}					t_data;

valgrind detecte des read incorrect et un leak de 1 bytes parfois, osef ?
---------------------------------
